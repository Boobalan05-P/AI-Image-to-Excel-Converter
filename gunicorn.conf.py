workers = 1
threads = 1
timeout = 180
graceful_timeout = 30

# Render starts Gunicorn from the repository root before applying --directory
# backend. Keep this configuration here so it is picked up by that command.
